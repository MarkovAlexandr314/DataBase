3)Нет. Первый вернет больше строк, т.к. сначала он вернет все строки из t1, а тем строкам, которым он не нашел пару в t2 он поставит NULL и только потом отфильтрует по условию t1.column1=0. А второй запрос на этапе объединения смотрит на то, что есть пара и t1.column1=0. Получается первый вернет дополнительные строки, где t1.column1=0 и во второй таблице проставлен NULL.
